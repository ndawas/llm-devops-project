services:
  floci:
    image: floci/floci:latest
    ports:
      - "4566:4566"
      - "4500:4500"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - FLOCI_STORAGE_MODE=hybrid
