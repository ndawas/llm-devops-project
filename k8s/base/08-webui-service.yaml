apiVersion: v1
kind: Service
metadata:
  name: webui-service
  namespace: llm-app
  labels:
    app: open-webui
spec:
  type: NodePort
  selector:
    app: open-webui
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
